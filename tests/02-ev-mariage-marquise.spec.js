'use strict';

const { test } = require('@playwright/test');

// assistant-evenement absent — relancer quand ev-widget.js arrive
test('2 — ev Mariage, 120 invités avec marquise au panier', async () => {
  test.skip(true, 'assistant-evenement absent — relancer quand ev-widget.js arrive');
});
